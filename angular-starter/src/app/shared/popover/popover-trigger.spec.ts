import { PopoverTrigger } from './popover-trigger';

describe('PopoverTrigger', () => {
  it('should create an instance', () => {
    const directive = new PopoverTrigger();
    expect(directive).toBeTruthy();
  });
});
